package TestCase::Lib::SPVM::Regex {
  use SPVM::Regex;
  
  sub compile : int () {
    my $re = SPVM::Regex->new("abc");
    
    return 1;
  }

=pod
  sub match : int () {
    my $re = SPVM::Regex->new("abc");
    
    my $str = "zabcz";
    
    my $matches = $re->match($str);
    
    unless ($matched) {
      return 0;
    }
    
    return 1;
  }
=cut
}
