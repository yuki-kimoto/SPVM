use strict;
use warnings;

use SPVM::Builder::Config;
my $config = SPVM::Builder::Config->new_gnu99;

# C souce files
my @source_files = qw(
  bar1.c
  bar2.c
);

$config->add_source_files(@source_files);

$config;
