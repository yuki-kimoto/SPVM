class TestCase::Lib::Int {
  use Fn;
  static method basic : int () {
    my $oval = Int->new(INT32_MIN());
    my $value1 = $oval->value;
    
    unless ($value1 == INT32_MIN()) {
      return 0;
    }
    
    return 1;
  }
}
