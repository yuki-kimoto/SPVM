class TestCase::Lib::Float {
  use Fn;
  
  static method basic : int () {
    my $oval = Float->new(FLT_MIN());
    my $value1 = $oval->value;
    
    unless ($value1 == FLT_MIN()) {
      return 0;
    }
    
    return 1;
  }
}
