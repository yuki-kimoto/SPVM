class TestCase::Lib::Long {
  use Fn;
  static method basic : int () {
    my $oval = Long->new(INT64_MIN());
    my $value1 = $oval->value;
    
    unless ($value1 == INT64_MIN()) {
      return 0;
    }
    
    return 1;
  }
}
