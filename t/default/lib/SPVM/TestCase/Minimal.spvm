package TestCase::Minimal {
  has x : int;
  has y : int;

  sub get_x : int ($self : TestCase::Minimal) {
    return $self->{x};
  }

  sub set_x : void ($self : TestCase::Minimal, $x : int) {
    $self->{x} = $x;
  }
  
  static sub new : TestCase::Minimal () {
    return new TestCase::Minimal;
  }
}
