package TestCase::Int {
  use Int;

  sub basic : int () {
    my $obj_value = Int->new(3);
    my $value = $obj_value->get_value;
    
    if ($value == 3) {
      return 1;
    }
    
    return 0;
  }
}
