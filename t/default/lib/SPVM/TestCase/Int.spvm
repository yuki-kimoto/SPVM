use Int;

package TestCase::Int {
  sub constant : int () {
    if (Int->MIN_VALUE() == -2147483648) {
      if (Int->MAX_VALUE() == 2147483647) {
        if (Int->BYTES() == 4) {
          if (Int->SIZE() == 32) {
            return 1;
          }
        }
      }
    }
    
    return 0;
  }
}
