class TestCase::Errno {
  use Point;
  static method basic : int () {
    
    {
      my $errno = errno;
      unless ($errno == -1) {
        return 0;
      }
      
      set_errno class_id Point;
      
      unless (errno >= 0) {
        return 0;
      }
    }
    
    return 1;
  }
}
