use strict;
use warnings;

use SPVM::Builder::Config;

my $config = SPVM::Builder::Config->new_c99;

$config->optimize('-O2');
$config->force(1);
$config->add_sources('foo.c', 'bar.c', 'baz/baz.c');

$config;
