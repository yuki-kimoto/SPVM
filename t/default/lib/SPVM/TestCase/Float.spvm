use Float;

package TestCase::Float {
  sub basic : int () {
    my $obj_value = Float->new(3);
    my $value = $obj_value->get_value;
    
    if ($value == 3) {
      return 1;
    }
    
    return 0;
  }
}
