use Float;

package TestCase::Float {
  sub basic : int () {
    my $obj_value = Float->new(0.25);
    my $value = $obj_value->get_value;
    
    if ($value == 0.25) {
      return 1;
    }
    
    return 0;
  }
}
