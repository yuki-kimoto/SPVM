use Double;
use Math;

package TestCase::Double {
  sub basic : int () {
    my $obj_value = Double->new(0.25);
    my $value = $obj_value->get_value;
    
    if ($value == 0.25) {
      return 1;
    }
    
    return 0;
  }
}
