package TestCase::Extension2 {
  static sub spvm_extension2 : int () {
    my $total = TestCase::Extension2::mul(5, 3);
    
    if ($total == 15) {
      return 1;
    }
    
    return 0;
  }

  static sub spvm_extension2_binding : int () {
    my $total = TestCase::Extension2::one();
    
    if ($total == 1) {
      return 1;
    }
    
    return 0;
  }

  static native sub mul : int ($num1 : int, $num2 : int);

  static native sub one : int ();
}
