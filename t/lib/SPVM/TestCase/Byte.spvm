use Byte;

package TestCase::Byte {
  sub constant : int () {
    if (Byte::MIN_VALUE() == (byte)-128) {
      if (Byte::MAX_VALUE() == (byte)127) {
        if (Byte::BYTES() == 1) {
          if (Byte::SIZE() == 8) {
            return 1;
          }
        }
      }
    }
    
    return 0;
  }
}
