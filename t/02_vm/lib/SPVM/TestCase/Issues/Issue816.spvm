class TestCase::Issues::Issue816 {
  
  use List;
  
  # https://github.com/yuki-kimoto/SPVM/issues/816
  static method test : int () {
    
    my $objects = (List of string)List->new(["foo"]);
    
    my $object = 1 ? $objects->[-1] : (string)undef;
    
    unless ($object eq "foo") {
      return 0;
    }
    
    return 1;
  }
}
