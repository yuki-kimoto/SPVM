class TestCase::Issues::Issue902 {
  use Complex_2d;
  
  has foo : public Int;
  
  private method add_task : void ($object : object) {
    
    $self->add_task([has number : int] method : void () {});
    
    if (1 || 1 || 1) {
      my $self = new TestCase::Issues::Issue902;
      $self->{foo} //= undef;
    }
  }
  
}
