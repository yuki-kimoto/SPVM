class TestCase::Definition::InitBlock {
  use TestCase::Empty;
  
  our $FOO : int;
  
  our $BAR : int;
  
  our $BAZ : int;
  
  INIT {
    $FOO = 1;
    
    # Call a default INIT block
    Int->INIT;
    TestCase::Empty->INIT;
    
    $BAZ = 1;
  }
  
  INIT {
    $BAR = 1;
    $BAZ = 2;
  }
  
  # Array store undef
  static method init_class_var : int () {
    
    unless ($FOO == 1) {
      return 0;
    }
    
    unless ($BAR == 1) {
      return 0;
    }
    
    unless ($BAZ == 2) {
      return 0;
    }
    
    return 1;
  }
}
