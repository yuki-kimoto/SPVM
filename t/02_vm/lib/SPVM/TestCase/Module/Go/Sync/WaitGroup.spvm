class TestCase::Module::Go::Sync::WaitGroup {
  
  use Go::Sync::WaitGroup;
  
  static method basic : int () {
    
    my $wg = Go::Sync::WaitGroup->new;
    
    $wg->add;
    
    $wg->done;
    
    $wg->add(1);
    
    $wg->done;
    
    $wg->add(2);
    
    $wg->done;
    
    $wg->done;
    
    $wg->wait;
    
    return 1;
  }
}
