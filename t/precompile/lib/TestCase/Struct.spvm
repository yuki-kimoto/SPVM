package TestCase::Struct : struct {
  native sub new : TestCase::Struct ($x : int);
  
  native sub get_x : int ($self : self);
  
  native sub DESTROY : void ($self : self);
  
  precompile sub struct_test : int () {
    my $struct = TestCase::Struct->new(3);
    my $x = $struct->get_x;
    
    if ($x == 3) {
      return 1;
    }
    
    return 0;
  }
}
