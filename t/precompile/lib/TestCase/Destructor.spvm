package TestCase::Destructor {
  use TestCase::Minimal;
  
  has minimal : TestCase::Minimal;
  
  precompile sub new : TestCase::Destructor () {
    return new TestCase::Destructor;
  }
  
  precompile sub DESTROY : void ($self : self) {
    $self->{minimal}->{x} = 3;
  }
}
