package TestCase::Minimal {
  has x : int;
  has y : int;

  jit sub get_x : int ($self : self) {
    return $self->{x};
  }

  jit sub set_x : void ($self : self, $x : int) {
    $self->{x} = $x;
  }
  
  jit sub new : TestCase::Minimal () {
    return new TestCase::Minimal;
  }
}
