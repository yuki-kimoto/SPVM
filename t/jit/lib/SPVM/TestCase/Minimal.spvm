package TestCase::Minimal {
  has x : int;
  has y : int;

  jit sub get_x : int ($self : TestCase::Minimal) {
    return $self->{x};
  }

  jit sub set_x : void ($self : TestCase::Minimal, $x : int) {
    $self->{x} = $x;
  }
  
  jit static sub new : TestCase::Minimal () {
    return new TestCase::Minimal;
  }
}
