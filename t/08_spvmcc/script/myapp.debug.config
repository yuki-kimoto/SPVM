my $config_global = SPVM::Builder::Config::Exe->load_base_config(__FILE__);

$config_global->build_type('Debug');

$config_global->add_before_compile_cb(sub {
  my ($config, $compile_info) = @_;
});

$config_global;
