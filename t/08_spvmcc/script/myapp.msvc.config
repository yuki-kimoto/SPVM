use SPVM::Builder::Config::Exe::MSVC;

my $config_global = SPVM::Builder::Config::Exe::MSVC->load_base_config(__FILE__);

$config_global->libs([]);

$config_global->optimize('-Od');

$config_global->apply;

$config_global;
