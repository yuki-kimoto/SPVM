use strict;
use warnings;
use Data::Dumper;

use SPVM::Builder::Config::Exe;

my $config = SPVM::Builder::Config::Exe->load_mode_config(__FILE__, 'no_precompile');

$config->no_compiler_api(1);

$config;
