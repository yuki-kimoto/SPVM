use strict;
use warnings;
use Data::Dumper;

use SPVM::Builder::Config::Exe;

my $bconf = SPVM::Builder::Config::Exe->new_gnu99;

$bconf->before_link(sub {
  my ($config, $objects) = @_;
  
  warn Dumper $objects;
  
  return $objects;
});

$bconf;
