use strict;
use warnings;

use SPVM::Builder::Config;

my $bconf = SPVM::Builder::Config->new_gnu99(file => __FILE__);

$bconf->optimize('-O2');

my @source_files = qw(
  foo.c
  bar.c
  baz/baz.c
);
$bconf->add_source_files(@source_files);

$bconf;
