use strict;
use warnings;

use SPVM::Builder::Config;

my $bconf = SPVM::Builder::Config->new_c99;

$bconf->optimize('-O2');
$bconf->force(1);

my @sources = qw(
  foo.c
  bar.c
  baz/baz.c
);
$bconf->add_sources(@sources);

$bconf;
