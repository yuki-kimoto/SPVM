use File::Basename 'dirname';

my $config_global = SPVM::Builder::Config::Exe->new;

my $config_dir = dirname __FILE__;

$config_global->use_spvm_archive("$ENV{SPVM_BUILD_DIR}/.tmp/spvm-archive-myapp");

$config_global->optimize('-O0 -g3');

$config_global;
