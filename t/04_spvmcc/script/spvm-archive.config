use File::Basename 'dirname';

my $config_exe = SPVM::Builder::Config::Exe->new;

my $config_dir = dirname __FILE__;

$config_exe->use_spvm_archive("$ENV{SPVM_BUILD_DIR}/.tmp/spvm-archive-myapp");

$config_exe;
