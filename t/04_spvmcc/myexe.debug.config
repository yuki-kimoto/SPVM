use strict;
use warnings;
use Data::Dumper;

use SPVM::Builder::Config::Exe;

my $config = SPVM::Builder::Config::Exe->load_base_config(__FILE__);

$config->add_before_each_compile_cb(sub {
  my ($config, $compile_info) = @_;
  
  $compile_info->optimize('-O0 -g');
});

$config;
