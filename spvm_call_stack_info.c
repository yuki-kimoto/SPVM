#include "spvm_call_stack_info.h"
#include "spvm_runtime.h"

SPVM_CALL_STACK_init_call_stack_info(SPVM_RUNTIME* runtime) {
  
  // Constant pool
  int32_t* constant_pool = runtime->constant_pool;
  
}
