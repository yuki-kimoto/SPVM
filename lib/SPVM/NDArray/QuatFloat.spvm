# SPVM::NDArray::QuatFloat is created by regen/regen_ndarray_quat.pl
package SPVM::NDArray::QuatFloat : public {
  use SPVM::Quat_4f;
  has base : public SPVM::Quat_4f[];
  has offset : public int;
  has length : public int;
  has T : public byte;
  has shape : public int[];
  has na_flags : public byte[];
}
