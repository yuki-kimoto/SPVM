package SPVM::NDArray::Long : public {
  has array : public long[];
  has length : public int;
  has offset : public int;
  has flag : public int;
  has dims : public int[];
  has elem_flags : public byte[];
}
