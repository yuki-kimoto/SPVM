# SPVM::NDArray::String is created by regen/regen_ndarray.pl
package SPVM::NDArray::String : public {
  has base : public string[];
  has offset : public int;
  has length : public int;
  has T : public byte;
  has shape : public int[];
  has na_flags : public byte[];
}
