package SPVM::NDArray::Float public {
  has array : public float[];
  has length : public int;
  has offset : public int;
  has flag : public int;
  has dims : public int[];
  has elem_flags : public byte[];
}
