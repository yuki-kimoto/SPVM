# SPVM::NDArray::Float is created by regen/regen_ndarray.pl
package SPVM::NDArray::Float : public {
  has base : public float[];
  has offset : public int;
  has length : public int;
  has T : public byte;
  has shape : public int[];
  has na_flags : public byte[];
}
