package SPVM::Bool {
  our $TRUE : ro SPVM::Bool;
  our $FALSE : ro SPVM::Bool;
  has val : ro int;
  
  BEGIN {
    # Singleton true
    $TRUE = new SPVM::Bool;
    $TRUE->{val} = 1;
    
    # Singleton false
    $FALSE = new SPVM::Bool;
    $FALSE->{val} = 0;
  }
}
