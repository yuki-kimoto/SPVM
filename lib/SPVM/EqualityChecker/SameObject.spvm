package SPVM::EqualityChecker::SameObject {
  sub new : SPVM::EqualityChecker::SameObject () {
    return new SPVM::EqualityChecker::SameObject;
  }
  
  sub : int ($self : self, $object1 : object, $object2 : object) {
    if ($object1 == $object2) {
      return 1;
    }
    else {
      return 0;
    }
  }
}
