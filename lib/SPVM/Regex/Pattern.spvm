class Regex::Pattern {
  allow Regex;
  
  enum {
    RE_START,
    RE_END,
    PATTERN,
    CAPTURE_START,
    CAPTURE_END,
  }
  has code_point_range : int[];
  has negate : byte;
  has min_repeat : int;
  has max_repeat : int;
  has capture_begin : byte;
  has capture_end : byte;
}
