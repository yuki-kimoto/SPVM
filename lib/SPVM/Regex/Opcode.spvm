class Regex::Opcode {
  allow Regex;
  
  enum {
    ID_CHAR,
    ID_JUMP,
    ID_BRANCH,
    ID_MATCH,
    ID_CAPTURE_BEGIN,
    ID_CAPTURE_END,
  }
  
  has id : int;
  has operand0 : int;
  has operand1 : int;
  has operand2 : int;
  has operand3 : int;
}
