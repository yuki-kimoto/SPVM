package Byte {
  has value : private byte;
  
  sub new : Byte ($class : class, $value : byte) {
    my $self = new Byte;
    
    $self->{value} = $value;
    
    return $self;
  }
  
  sub get_value : byte ($self : self) {
    return $self->{value};
  }
}
