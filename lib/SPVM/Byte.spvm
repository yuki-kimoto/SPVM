package Byte : private {
  has value : private byte;
  
  sub new : Byte ($value : byte) {
    my $self = new Byte;
    
    $self->{value} = $value;
    
    return $self;
  }
  
  sub get_value : byte ($self : self) {
    return $self->{value};
  }
}
