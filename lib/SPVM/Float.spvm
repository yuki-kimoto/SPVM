package Float {

  has value : float;

  enum {
    BYTES = 4,
    SIZE = 32,
    MAX_EXPONENT = 127,
    MIN_EXPONENT = -126,
  }
  
  native sub POSITIVE_INFINITY : float ();
  native sub NEGATIVE_INFINITY : float ();
  native sub NaN : float ();
  
  native sub MAX_VALUE : float ();
  native sub MIN_NORMAL : float();
  native sub MIN_VALUE : float();
  
  native sub is_infinite : int($x : float);
  native sub is_finite : int($x : float);
  native sub is_nan : int ($x : float);
  native sub float_to_int_bits : int ($x : float);
  native sub float_to_raw_int_bits : int ($x : float);
  native sub int_bits_to_float : float ($x : int);
}
