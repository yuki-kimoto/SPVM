package Float : private {
  has value : private float;
  
  sub new : Float ($class : class, $value : float) {
    my $self = new Float;
    
    $self->{value} = $value;
    
    return $self;
  }
  
  sub get_value : float ($self : self) {
    return $self->{value};
  }
}
