#include "spvm_native.h"

int32_t SPVM_NATIVE_SPVM__FileHandle__new(SPVM_ENV* env, SPVM_VALUE* stack) {
  
  return SPVM_SUCCESS;
}

int32_t SPVM_NATIVE_SPVM__FileHandle__DESTROY(SPVM_ENV* env, SPVM_VALUE* stack) {
  
  return SPVM_SUCCESS;
}
