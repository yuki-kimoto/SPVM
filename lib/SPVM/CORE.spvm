package CORE {
  use IO;
  use Time;

  sub print : void ($value : string) { IO->print($value); }
  sub warn : void ($value : string) { IO->warn($value); }
  
  sub time : long () { return Time->time(); }
}
