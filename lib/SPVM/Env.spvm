class Env : pointer {
  use Runtime;
  
  # Fields
  has runtime : Runtime;
  
  # Class Methods
  native static method new : Env ($env : Runtime);
  
  # Instance Methods
  native method DESTROY : void ();
}
