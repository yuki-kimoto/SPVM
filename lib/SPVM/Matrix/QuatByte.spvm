# SPVM::Matrix::QuatByte is created by regen/regen_matrix.pl
package SPVM::Matrix::QuatByte {
  has values : ro SPVM::Quat_4b[];
  has row : ro int;
  has col : ro int;
  
  sub new : SPVM::Matrix::QuatByte ($values : SPVM::Quat_4b[], $row : int, $col : int) {
    unless ($values) {
      die "Values must defined";
    }

    unless ($row * $col == scalar @$values) {
      die "Values length must be row * col";
    }
    my $matrix = new SPVM::Matrix::QuatByte;
    $matrix->{values} = $values;
    $matrix->{row} = $row;
    $matrix->{col} = $col;
    return $matrix;
  }
}
