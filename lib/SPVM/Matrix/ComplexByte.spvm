# SPVM::Matrix::ComplexByte is created by regen/regen_matrix.pl
package SPVM::Matrix::ComplexByte {
  has values : ro SPVM::Complex_2b[];
  has row : ro int;
  has col : ro int;
  
  sub new : SPVM::Matrix::ComplexByte ($values : SPVM::Complex_2b[], $row : int, $col : int) {
    unless ($values) {
      die "Values must defined";
    }

    unless ($row * $col == scalar @$values) {
      die "Values length must be row * col";
    }
    my $matrix = new SPVM::Matrix::ComplexByte;
    $matrix->{values} = $values;
    $matrix->{row} = $row;
    $matrix->{col} = $col;
    return $matrix;
  }
}
