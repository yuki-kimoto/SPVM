# Copyright (c) 2023 Yuki Kimoto
# MIT License

class Go {
  use Go::Callback;
  use Go::Channel::ReadCallback;
  
  our $GO : Go;
  
  INIT {
    $GO = new Go;
  }
  
  static method new () {
    return $GO;
  }
  
  method go : void ($cb : Go::Callback) {
    
  }
  
  method make : Go::Channel ($length : int = 0) {
    
  }
  
  method read : object () {
    
  }
  
  method write : void ($data : object) {
    
  }
  
  method select : void ($cases : object[], $default : Go::Channel::ReadCallback = undef) {
    
  }
  
  private static method getenv : string ($name : string) {
    
  }
}

