package Std {
  use Std::IO;
  use Std::Time;

  sub print : void ($value : string) { Std::IO->print($value); }
  sub warn : void ($value : string) { Std::IO->warn($value); }
  
  sub time : long () { return Std::Time->time(); }
}
