use Std::IO;
use Std::Time;

package Std {
  sub print : void ($value : String) { return Std::IO->print($value); }
  sub warn : void ($value : String) { return Std::IO->warn($value); }
  
  sub time : long () { return Std::Time->time(); }
}
