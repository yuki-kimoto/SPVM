use strict;
use warnings;

use SPVM::Builder::Config;

my $config = SPVM::Builder::Config->new_c99;

my $lib_dirs = $config->search_lib_dirs;
$config->add_lib_dirs(@$lib_dirs);

$config->add_dynamic_libs('z');

$config;
