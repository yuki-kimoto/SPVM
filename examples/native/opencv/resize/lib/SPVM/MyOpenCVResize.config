use strict;
use warnings;

use SPVM::Builder::Config;

my $config = SPVM::Builder::Config->new_cpp(file => __FILE__);

$config->add_lib('opencv_core', 'opencv_imgcodecs', 'opencv_highgui');

$config;
