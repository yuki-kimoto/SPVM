use strict;
use warnings;

use SPVM::Builder::Config;
my $bconf = SPVM::Builder::Config->new_cpp;
$bconf->push_libs('opencv_core', 'opencv_imgcodecs', 'opencv_highgui');

# $bconf->quiet(0);

$bconf;
