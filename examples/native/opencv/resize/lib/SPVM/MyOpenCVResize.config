use strict;
use warnings;

use SPVM::Builder::Config;
my $config = SPVM::Builder::Config->new_cpp;
$config->push_libs('opencv_core', 'opencv_imgcodecs', 'opencv_highgui');

# $config->quiet(0);

$config;
