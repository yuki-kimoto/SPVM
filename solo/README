This is SPVM solo test command

   # Normal run
   yacc/bison.sh && perl Makefile.PL && make && perl solo/solo_Makefile.PL && make -f solo/Makefile && ./solo/spvm_main TestCase

   # Debug run - Print AST, package information, operaion codes
   yacc/bison.sh && perl Makefile.PL && make && perl solo/solo_Makefile.PL --debug-dump --debug-object-count && make -f solo/Makefile && ./solo/spvm_main TestCase

   # Debug run - Print yacc result
   yacc/bison.sh && perl Makefile.PL && make && perl solo/solo_Makefile.PL --debug-yacc && make -f solo/Makefile && ./solo/spvm_main TestCase
 