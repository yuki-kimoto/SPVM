package TestCase::Inline {
  use TestCase::Inline;

  native sub sum : int ($num1 : int, $num2 : int);
}

__NATIVE__

#include "spvm_api.h"

int32_t SPVM__Test__Inline(SPVM_ENV api, SPVM_VALUE* args) {
  int32_t total = args[0].ival + args[1].ival;
  
  return total;
}
