class TestCase::CommonInterface : interface_t {
  use TestCase::Minimal;
  
  required method shared1 : string ();
  
  method shared2 : string ();

  method shared3 : string ();

  method shared4 : string ($minimal : TestCase::Minimal);
}
